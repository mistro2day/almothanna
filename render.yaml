services:
  - type: web
    name: almothanna-backend
    region: oregon
    plan: free
    env: node
    buildCommand: npm install && npm run build
    startCommand: node dist/main.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000